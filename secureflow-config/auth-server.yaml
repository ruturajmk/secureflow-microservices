server:
  port: 9000

spring:
  application:
    name: auth-server

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    org.springframework.security: INFO