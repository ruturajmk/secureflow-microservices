spring:

  security:
    oauth2:
      authorizationserver:
        issuer: https://auth.company.com
        
auth:
  issuer: http://localhost:9000

eureka:
  client:
    service-url:
      defaultZone: https://eureka.company.com/eureka

management:
  zipkin:
    tracing:
      endpoint: https://zipkin.company.com/api/v2/spans