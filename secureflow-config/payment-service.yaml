server:
  port: 8085

spring:
  application:
    name: payment-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: INFO
    org.springframework.kafka: INFO
    org.springframework.security: INFO