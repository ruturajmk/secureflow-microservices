apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: secureflow
spec:
  selector:
    app: inventory-service
  ports:
    - port: 8086
      targetPort: 8086
  type: ClusterIP
